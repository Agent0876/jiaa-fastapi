apiVersion: v1
kind: Service
metadata:
  name: jiaa-ai-judge-service-svc
  labels:
    app: jiaa-ai-judge-service
    service: ai-judge-service
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: jiaa-ai-judge-service

